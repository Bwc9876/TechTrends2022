---
import { Card } from "react-bootstrap";

export interface Props {
    name: string;
}

const { name } = Astro.props;
---

<>
    <Card.Title className="gradient-text" as="h3">{name}</Card.Title>
    <slot />
</>
