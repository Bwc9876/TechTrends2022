---
import { Row, Col } from 'react-bootstrap';

const { className, colClassName } = Astro.props;
---

<Row className={className}>
    <Col className={colClassName}>
        <slot/>
    </Col>
</Row>
